#include <iostream>
#include <set>

using namespace std;

int main() {
    set<int> remains;
    int num;

    for (int i = 0; i < 10; i++) {
        cin >> num;
        remains.insert(num % 42);
    }

    cout << remains.size();

    return 0;
}